(function () {
    'use strict';
    var root = document.querySelector('[data-sc-archive]'); if (!root) return; var isRecordings = root.getAttribute('data-recordings') === '1', streamId = root.getAttribute('data-stream-id') || '0', dialog = root.querySelector('[data-sc-archive-player]'), frame = dialog.querySelector('iframe');
    root.querySelectorAll('[data-sc-archive-row]').forEach(function (row) { row.querySelector('[data-sc-archive-play]').addEventListener('click', function () { var created = row.getAttribute('data-created-id'), url = isRecordings ? (created ? 'player?type=movie&id=' + encodeURIComponent(created) + '&container=mp4' : '') : 'player?type=timeshift&id=' + encodeURIComponent(streamId) + '&start=' + encodeURIComponent(row.getAttribute('data-start')) + '&duration=' + encodeURIComponent(row.getAttribute('data-duration')); if (!url) { this.disabled = true; return; } frame.src = url; if (typeof dialog.showModal === 'function') dialog.showModal(); else window.open(url, '_blank', 'noopener'); }); var remove = row.querySelector('[data-sc-archive-delete]'); if (remove) remove.addEventListener('click', function () { if (!window.confirm('Cancel and delete this recording?')) return; remove.disabled = true; fetch('api?action=delete_recording&id=' + encodeURIComponent(row.getAttribute('data-recording-id')), { credentials: 'same-origin', headers: { Accept: 'application/json', 'X-Requested-With': 'XMLHttpRequest' } }).then(function (r) { return r.json(); }).then(function (data) { if (data.result === true) row.remove(); else { remove.disabled = false; window.alert('The recording could not be deleted.'); } }).catch(function () { remove.disabled = false; window.alert('The delete request failed.'); }); }); }); root.querySelector('[data-sc-archive-close]').addEventListener('click', function () { if (typeof dialog.close === 'function') dialog.close(); frame.src = 'about:blank'; });
}());
