(function () {
    'use strict';
    var form = document.querySelector('[data-sc-quick-form]'); if (!form) return; form.addEventListener('submit', function (event) { event.preventDefault(); var submitter = event.submitter; if (!submitter || !submitter.name) return; if (!window.confirm('Are you sure? This action cannot be undone.')) return; var button = submitter, payload = new FormData(); payload.append(submitter.name, submitter.value || 'Run'); button.disabled = true; var result = form.querySelector('[data-sc-quick-result]'); result.textContent = 'Running…'; fetch(form.action, { method: 'POST', body: payload, credentials: 'same-origin', headers: { Accept: 'application/json', 'X-Requested-With': 'XMLHttpRequest' } }).then(function (r) { return r.text(); }).then(function (text) { var data = {}; try { data = JSON.parse(text); } catch (e) {} if (data.result === true) result.textContent = 'Action completed.'; else result.textContent = 'The action was rejected. Retry when the server is available.'; }).catch(function () { result.textContent = 'The action failed. Retry.'; }).finally(function () { button.disabled = false; }); });
}());
