(function () { 'use strict'; var root = document.querySelector('[data-sc-process-monitor]'); if (!root) return; var input = root.querySelector('[data-sc-process-search]'); if (!input) return; input.addEventListener('input', function () { var q = input.value.trim().toLowerCase(); root.querySelectorAll('[data-sc-process-row]').forEach(function (row) { row.hidden = q !== '' && (row.getAttribute('data-search') || '').indexOf(q) === -1; }); }); }());
