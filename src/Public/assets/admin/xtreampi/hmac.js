(function(){'use strict';var root=document.querySelector('[data-sc-hmac-editor]'),form=root&&root.querySelector('form');if(!form)return;var key=form.querySelector('[data-sc-hmac-key]');function generate(){var chars='ABCDEF0123456789',value='';for(var i=0;i<32;i++)value+=chars[Math.floor(Math.random()*chars.length)];key.value=value}form.querySelector('[data-sc-generate]').onclick=generate;if(!key.value)generate();form.onsubmit=function(e){e.preventDefault();var error=form.querySelector('[data-sc-hmac-error]'),submit=form.querySelector('[type=submit]');error.hidden=true;submit.disabled=true;fetch(form.action,{method:'POST',body:new FormData(form),credentials:'same-origin',headers:{Accept:'application/json, text/javascript, */*; q=0.01','X-Requested-With':'XMLHttpRequest'}}).then(function(r){return r.text()}).then(function(text){var result;try{result=JSON.parse(text)}catch(ignore){result=null}if(result&&result.location){location.href=result.location;return}submit.disabled=false;error.textContent=result&&String(result.status)===form.dataset.statusExists?'This HMAC key already exists. Generate another key.':result&&result.message?result.message:'An error occurred while processing your request.';error.hidden=false}).catch(function(){submit.disabled=false;error.textContent='The HMAC key could not be saved.';error.hidden=false})};}());
