(function () { 'use strict'; var root = document.querySelector('[data-sc-epg-editor]'), form = root && root.querySelector('form'); if (!form) return; form.onsubmit = function (event) { event.preventDefault(); var error = form.querySelector('[data-sc-epg-error]'), submit = form.querySelector('[type=submit]'); error.hidden = true; submit.disabled = true; fetch(form.action, { method: 'POST', body: new FormData(form), credentials: 'same-origin', headers: { Accept: 'application/json, text/javascript, */*; q=0.01', 'X-Requested-With': 'XMLHttpRequest' } }).then(function (response) { return response.text(); }).then(function (text) { var result; try { result = JSON.parse(text); } catch (ignore) { result = null; } if (result && result.location) { location.href = result.location; return; } submit.disabled = false; error.textContent = 'The EPG could not be saved. Check the source and try again.'; error.hidden = false; }).catch(function () { submit.disabled = false; error.textContent = 'The EPG could not be saved. Please try again.'; error.hidden = false; }); }; }());
