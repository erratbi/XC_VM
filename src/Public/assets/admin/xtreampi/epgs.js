(function () { 'use strict'; var root = document.querySelector('[data-sc-epgs]'); if (!root) return; var search = root.querySelector('[data-sc-epg-search]'), rows = Array.from(root.querySelectorAll('[data-sc-epg-row]')), empty = root.querySelector('[data-sc-epg-empty]'); search.oninput = function () { var term = search.value.trim().toLowerCase(), visible = 0; rows.forEach(function (row) { var matches = !term || row.dataset.search.indexOf(term) !== -1; row.hidden = !matches; if (matches) visible++; }); empty.hidden = visible > 0; empty.querySelector('td').textContent = term ? 'No EPG files match this search.' : 'No EPG files have been added yet.'; }; function action(type, id, button) { if (type === 'delete' && !confirm('Delete this EPG file?')) return; button.disabled = true; fetch('./api?action=epg&sub=' + encodeURIComponent(type) + '&epg_id=' + encodeURIComponent(id), { credentials: 'same-origin', headers: { Accept: 'application/json', 'X-Requested-With': 'XMLHttpRequest' } }).then(function (response) { return response.json(); }).then(function (data) { if (data.result !== true) throw new Error(); if (type === 'delete') { var row = button.closest('[data-sc-epg-row]'); row.remove(); rows = Array.from(root.querySelectorAll('[data-sc-epg-row]')); search.dispatchEvent(new Event('input')); } else button.disabled = false; }).catch(function () { button.disabled = false; alert('The EPG action could not be completed.'); }); } root.addEventListener('click', function (event) { var button = event.target.closest('[data-sc-epg-action]'); if (button) action(button.dataset.scEpgAction, button.dataset.scEpgId, button); var force = event.target.closest('[data-sc-epg-force]'); if (force) { force.disabled = true; fetch('./api?action=force_epg', { credentials: 'same-origin' }).finally(function () { force.disabled = false; }); } }); }());
