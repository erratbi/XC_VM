(function () { 'use strict'; var root = document.querySelector('[data-sc-bouquet-order]'), form = root && root.querySelector('form'), list = root && root.querySelector('[data-sc-bouquet-order-list]'); if (!form || !list) return; var dragging; function update() { Array.from(list.children).forEach(function (item, index) { item.querySelector('[data-sc-bouquet-order-position]').textContent = String(index + 1); }); } list.addEventListener('dragstart', function (event) { var item = event.target.closest('li'); if (!item) return; dragging = item; item.classList.add('is-dragging'); event.dataTransfer.effectAllowed = 'move'; }); list.addEventListener('dragover', function (event) { var target = event.target.closest('li'); if (!dragging || !target || dragging === target) return; event.preventDefault(); var box = target.getBoundingClientRect(); list.insertBefore(dragging, event.clientY < box.top + box.height / 2 ? target : target.nextElementSibling); }); list.addEventListener('dragend', function () { if (dragging) dragging.classList.remove('is-dragging'); dragging = null; update(); }); root.querySelector('[data-sc-bouquet-sort-name]').onclick = function () { Array.from(list.children).sort(function (a, b) { return a.querySelector('strong').textContent.localeCompare(b.querySelector('strong').textContent); }).forEach(function (item) { list.append(item); }); update(); }; form.onsubmit = function (event) { event.preventDefault(); var submit = form.querySelector('[type=submit]'), error = form.querySelector('[data-sc-bouquet-order-error]'); if (form.elements.confirmReplace.checked && !confirm('Update the bouquet order for every existing line and package?')) return; form.elements.bouquet_order_array.value = JSON.stringify(Array.from(list.children).map(function (item) { return Number(item.dataset.bouquetId); })); error.hidden = true; submit.disabled = true; fetch(form.action, { method: 'POST', body: new FormData(form), credentials: 'same-origin', headers: { Accept: 'application/json, text/javascript, */*; q=0.01', 'X-Requested-With': 'XMLHttpRequest' } }).then(function (response) { return response.text(); }).then(function (text) { var result; try { result = JSON.parse(text); } catch (ignore) { result = null; } if (result && result.location) { location.href = result.location; return; } submit.disabled = false; error.textContent = 'The bouquet order could not be saved.'; error.hidden = false; }).catch(function () { submit.disabled = false; error.textContent = 'The bouquet order could not be saved.'; error.hidden = false; }); }; update(); }());
