<?php

use XcVm\Core\Auth\Authorization;
use XcVm\Domain\Stream\ConnectionTracker;

$streamcreedPageScripts = ['assets/streamcreed/proxies.js'];
$streamcreedCanEdit = Authorization::check('adv', 'edit_server');
$streamcreedCanConnections = Authorization::check('adv', 'live_connections');
$streamcreedProxies = array_filter($rServers ?? [], static fn(array $server): bool => (int) ($server['server_type'] ?? 0) === 1);
$streamcreedStatus = static function (array $server): array { if (empty($server['enabled'])) return ['disabled', 'Disabled']; if (!empty($server['server_online'])) return ['online', 'Online']; return match ((int) ($server['status'] ?? 0)) { 3, 5 => ['installing', 'Provisioning'], 4 => ['warning', 'Install failed'], default => ['offline', 'Offline'] }; };
?>
<section class="sc-inventory" data-sc-proxy-inventory>
    <div class="sc-page-heading"><div><p class="sc-eyebrow">Infrastructure</p><h1>Proxies</h1></div><div class="sc-page-actions"><a class="sc-button sc-button-secondary" href="servers"><i class="fe-chevron-left" aria-hidden="true"></i> Back to servers</a><a class="sc-button sc-button-primary" href="server_install?proxy"><i class="fe-plus" aria-hidden="true"></i> Install proxy</a></div></div>
    <div class="sc-toolbar"><label class="sc-search-field"><i class="fe-search" aria-hidden="true"></i><span class="sc-visually-hidden">Search proxies</span><input type="search" placeholder="Search proxy name, domain, or IP" data-sc-proxy-search></label><label class="sc-filter-field"><span>Status</span><select data-sc-proxy-status><option value="all">All statuses</option><option value="online">Online</option><option value="offline">Offline</option><option value="disabled">Disabled</option><option value="installing">Provisioning</option><option value="warning">Install failed</option></select></label></div>
    <div class="sc-inventory-grid"><?php foreach ($streamcreedProxies as $streamcreedProxy): [$streamcreedStatusKey, $streamcreedStatusLabel] = $streamcreedStatus($streamcreedProxy); $streamcreedWatchdog = json_decode((string) ($streamcreedProxy['watchdog_data'] ?? ''), true) ?: []; $streamcreedParentIds = is_array($streamcreedProxy['parent_id'] ?? null) ? $streamcreedProxy['parent_id'] : (json_decode((string) ($streamcreedProxy['parent_id'] ?? '[]'), true) ?: []); $streamcreedParents = array_values(array_filter(array_map(static fn($id) => ($rServers[(int) $id]['server_name'] ?? null), $streamcreedParentIds))); $streamcreedConnections = (int) ConnectionTracker::getLiveConnections((int) $streamcreedProxy['id'], true); $streamcreedDomain = trim(explode(',', (string) ($streamcreedProxy['domain_name'] ?? ''))[0]); $streamcreedSearch = strtolower(implode(' ', [$streamcreedProxy['server_name'] ?? '', $streamcreedProxy['server_ip'] ?? '', $streamcreedDomain])); ?><article class="sc-inventory-card" data-sc-proxy-card data-status="<?php echo $streamcreedStatusKey; ?>" data-search="<?php echo htmlspecialchars($streamcreedSearch, ENT_QUOTES, 'UTF-8'); ?>"><header class="sc-inventory-card-header"><div class="sc-server-identity"><span class="sc-status-dot is-<?php echo $streamcreedStatusKey; ?>" aria-hidden="true"></span><div><h2><?php echo htmlspecialchars((string) $streamcreedProxy['server_name'], ENT_QUOTES, 'UTF-8'); ?></h2><p>Proxy server</p></div></div><span class="sc-status-badge is-<?php echo $streamcreedStatusKey; ?>"><?php echo $streamcreedStatusLabel; ?></span></header><div class="sc-inventory-card-body"><div class="sc-server-addresses"><div><span>Public IP</span><strong><?php echo htmlspecialchars((string) ($streamcreedProxy['server_ip'] ?? '—'), ENT_QUOTES, 'UTF-8'); ?></strong></div><div><span>Domain</span><strong><?php echo $streamcreedDomain !== '' ? htmlspecialchars($streamcreedDomain, ENT_QUOTES, 'UTF-8') : '—'; ?></strong></div></div><div class="sc-server-kpis"><div><span>Connections</span><strong><?php echo number_format($streamcreedConnections); ?><small> / <?php echo number_format((int) ($streamcreedProxy['total_clients'] ?? 0)); ?></small></strong></div><div><span>CPU</span><strong><?php echo !empty($streamcreedProxy['server_online']) ? (int) ($streamcreedWatchdog['cpu'] ?? 0) : 0; ?>%</strong></div><div><span>RAM</span><strong><?php echo !empty($streamcreedProxy['server_online']) ? (int) ($streamcreedWatchdog['total_mem_used_percent'] ?? 0) : 0; ?>%</strong></div><div><span>Latency</span><strong><?php echo !empty($streamcreedProxy['server_online']) ? number_format((float) ($streamcreedProxy['ping'] ?? 0), 0) : '—'; ?> <small>ms</small></strong></div></div><div class="sc-server-addresses"><div><span>Coverage</span><strong><?php echo htmlspecialchars(implode(', ', $streamcreedParents) ?: 'No servers selected', ENT_QUOTES, 'UTF-8'); ?></strong></div><div><span>Capacity</span><strong><?php echo number_format((float) ($streamcreedProxy['network_guaranteed_speed'] ?? 0), 0); ?> Mbps</strong></div></div></div><footer class="sc-inventory-card-footer"><span>#<?php echo (int) $streamcreedProxy['id']; ?></span><div><?php if ($streamcreedCanConnections): ?><a href="live_connections?server=<?php echo (int) $streamcreedProxy['id']; ?>">Connections</a><?php endif; ?><a href="server_view?id=<?php echo (int) $streamcreedProxy['id']; ?>">Monitor</a><?php if ($streamcreedCanEdit): ?><a class="sc-card-primary-action" href="proxy?id=<?php echo (int) $streamcreedProxy['id']; ?>">Edit</a><?php endif; ?></div></footer></article><?php endforeach; ?></div><div class="sc-data-panel sc-proxy-empty-panel" data-sc-proxy-empty<?php echo $streamcreedProxies ? ' hidden' : ''; ?>><div class="sc-table-scroll"><table class="sc-data-table"><tbody><tr><td class="sc-table-state">No proxies have been installed yet.</td></tr></tbody></table></div></div>
</section>
